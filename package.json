{
  "name": "lynx-flappy-bird",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rspeedy build",
    "build:website": "rspeedy build && cd website && rsbuild build",
    "dev": "rspeedy dev",
    "dev:website": "rspeedy build && cd website && rsbuild dev",
    "preview": "npm run build:website && npx serve website/dist"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "vue-lynx": "0.4.0"
  },
  "devDependencies": {
    "@lynx-js/qrcode-rsbuild-plugin": "^0.4.6",
    "@lynx-js/rspeedy": "^0.13.5",
    "@rsbuild/plugin-vue": "^1.2.6",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
